naftiko: 1.0.0-alpha2
info:
  label: LangSmith — ownerships
  description: 'LangSmith — ownerships. 3 operations. Lead operation: List Repo Owners. Self-contained Naftiko capability
    covering one Langchain business surface.'
  tags:
  - Langchain
  - ownerships
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LANGCHAIN_API_KEY: LANGCHAIN_API_KEY
capability:
  consumes:
  - type: http
    namespace: langchain-ownerships
    baseUri: ''
    description: LangSmith — ownerships business capability. Self-contained, no shared references.
    resources:
    - name: api-v1-repos-owner-repo-owners
      path: /api/v1/repos/{owner}/{repo}/owners
      operations:
      - name: listrepoownersapiv1reposownerrepoownersget
        method: GET
        description: List Repo Owners
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: owner
          in: path
          type: string
          required: true
        - name: repo
          in: path
          type: string
          required: true
      - name: addrepoownerapiv1reposownerrepoownerspost
        method: POST
        description: Add Repo Owner
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: owner
          in: path
          type: string
          required: true
        - name: repo
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: removerepoownerapiv1reposownerrepoownersdelete
        method: DELETE
        description: Remove Repo Owner
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: owner
          in: path
          type: string
          required: true
        - name: repo
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.LANGCHAIN_API_KEY}}'
  exposes:
  - type: rest
    namespace: langchain-ownerships-rest
    port: 8080
    description: REST adapter for LangSmith — ownerships. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/v1/repos/{owner}/{repo}/owners
      name: api-v1-repos-owner-repo-owners
      description: REST surface for api-v1-repos-owner-repo-owners.
      operations:
      - method: GET
        name: listrepoownersapiv1reposownerrepoownersget
        description: List Repo Owners
        call: langchain-ownerships.listrepoownersapiv1reposownerrepoownersget
        with:
          owner: rest.owner
          repo: rest.repo
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: addrepoownerapiv1reposownerrepoownerspost
        description: Add Repo Owner
        call: langchain-ownerships.addrepoownerapiv1reposownerrepoownerspost
        with:
          owner: rest.owner
          repo: rest.repo
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: removerepoownerapiv1reposownerrepoownersdelete
        description: Remove Repo Owner
        call: langchain-ownerships.removerepoownerapiv1reposownerrepoownersdelete
        with:
          owner: rest.owner
          repo: rest.repo
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: langchain-ownerships-mcp
    port: 9090
    transport: http
    description: MCP adapter for LangSmith — ownerships. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: list-repo-owners
      description: List Repo Owners
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: langchain-ownerships.listrepoownersapiv1reposownerrepoownersget
      with:
        owner: tools.owner
        repo: tools.repo
      outputParameters:
      - type: object
        mapping: $.
    - name: add-repo-owner
      description: Add Repo Owner
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: langchain-ownerships.addrepoownerapiv1reposownerrepoownerspost
      with:
        owner: tools.owner
        repo: tools.repo
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: remove-repo-owner
      description: Remove Repo Owner
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: langchain-ownerships.removerepoownerapiv1reposownerrepoownersdelete
      with:
        owner: tools.owner
        repo: tools.repo
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
